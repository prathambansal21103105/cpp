#include<iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
    string a;
    a="Pratham";
    cout<<a.size()<<endl;
    int input[100];
    cout<<input<<endl; //when you just print the name of your array it gives the address of the zeroth element.
    return 0;
}

// address is always stored in hexadecimal format.

// sizeof() gives the size of integers doubles arrays 